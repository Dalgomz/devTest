# Backend

Simple Flask backend for the Endpoint test.

## Setup

```bash
cd backend
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
flask --app app run --debug
```

The API is available at `http://127.0.0.1:5000`.
